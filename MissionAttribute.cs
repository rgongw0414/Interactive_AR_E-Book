using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using UnityEngine.EventSystems;
public class MissionAttribute : MonoBehaviour
{
    bool flag_MissionThree = false;
    bool good_ingredient_click = false;
    bool bad_ingredient_click = false;
    public int enough;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
